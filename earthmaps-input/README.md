# Private Earth map inputs

Place the real, owner-supplied archives here as `earth-16k-clean.xfmap` and
`earth-16k-populated.xfmap`, or point the Gradle properties at files elsewhere.
The archives are ignored by Git. Do not fabricate or commit either export.
